apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: frontend
  namespace: frontend

  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: hsbc
    environment: prod

spec:
  minAvailable: 2

  selector:
    matchLabels:
      app: frontend
