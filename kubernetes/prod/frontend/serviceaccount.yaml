apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend
  namespace: frontend

  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: hsbc
    environment: prod

automountServiceAccountToken: false
